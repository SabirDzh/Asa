# Copy this file to config/private.env and replace the placeholder.
# Keep config/private.env owner-only (chmod 600) and never commit it.
# This is a public API address, not a server/database/admin secret.
DIAGNOSTICS_ENDPOINT=https://your-diagnostics-domain.example/api/reports
